[project]
name = "ai4groundwater-tracker-python"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
    "fastapi",
    "pydantic",
    "numpy",
    "rasterio==1.5.1",
    "matplotlib",
    "kulfo @ git+https://github.com/DemisoDaba/kulfogw.git"
]

[tool.setuptools]
py-modules = []